# Cursor Rules

Sharing some of my cursor configs as I adjust and find what works specific to my development needs.

## Setup

I like to use markdown formatting in my `.cursorrules` file, which requires this setting override,
```json
"files.associations": {
    ".cursorrules": "markdown"
}
```
